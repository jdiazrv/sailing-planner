import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      subtitle="Preparando timeline, escalas, visitas y mapa."
      title="Cargando panel"
    />
  );
}
