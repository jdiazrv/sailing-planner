import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      subtitle="Sincronizando barcos visibles y comparativa compartida."
      title="Cargando vista compartida"
    />
  );
}
